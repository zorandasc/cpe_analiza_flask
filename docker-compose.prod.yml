#--------------------------------------------------
# START ALREADY BUILD DOCKER IMAGES
#---------------------------------------------------
services:
  db:
    image: postgres:16
    restart: unless-stopped
    # This tells Postgres to start with a specific timezone setting
    command: ["postgres", "-c", "timezone=Europe/Belgrade"]
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_NAME}
      TZ: Europe/Belgrade
    ports:
      - "5431:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      #WAIT UNTIL POSTGRES IS READY THAN START FLASK OR PGADMIN
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  flask:
    image: cpe-analiza-flask:latest
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      TZ: Europe/Belgrade
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
    depends_on:
      db:
        condition: service_healthy

  flask_test:
    image: cpe-analiza-flask:latest
    restart: unless-stopped
    ports:
      - "4000:5000"   # different port
    env_file:
      - .env
    environment:
      TZ: Europe/Belgrade
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}_test  # point to test DB
    depends_on:
      db:
        condition: service_healthy

  pgadmin:
    image: dpage/pgadmin4
    restart: unless-stopped
    environment:
      TZ: Europe/Belgrade
      PGADMIN_DEFAULT_EMAIL: ${PG_ADMIN_MAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PG_ADMIN_PASS}
    ports:
      - "5050:80"
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
